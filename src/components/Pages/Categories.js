import React from "react";
// import TagsElement from "../Tags/TagsElement/TagsElement";
// import Aux from "../../hoc/Aux";

const categories = ({ data }) => <div>Tags</div>;
// const itemsTags = data.map((itemTags, i) => {
//   return <TagsElement key={i} info={itemTags} />;
// });

// return (
//   <Aux>
//     <div className="">{itemsTags}</div>
//   </Aux>
// );

export default categories;
